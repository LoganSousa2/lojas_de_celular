<%= form_with(model: venda) do |form| %>
  <% if venda.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(venda.errors.count, "error") %> prohibited this venda from being saved:</h2>

      <ul>
        <% venda.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="row">
       <div class="col-lg-4 form-group">   
           <%= form.label :valor, :class => 'control-label required' %>
           <%= form.text_field :valor,  :class => 'form-control' %>
       </div>
       <div class="col-lg-4 form-group">   
           <%= form.label :data_venda, :class => 'control-label required' %>
           <%= form.text_field :data_venda,  :class => 'form-control' %>
       </div>

       <div class="col-lg-4 form-group">   
       <%= form.label :total_vendido, :class => 'control-label' %>
       <%= form.text_field :total_vendido, :class => 'form-control' %>
   </div>
   </div>
   <div class="row">
     <div class="col-lg-4 form-group">   
        <%= form.label :produto_id, :class => 'control-label required' %>
        <%= form.text_field :produto_id, :class => 'form-control', :onkeypress=>"mask(this, mphone);", :onblur=>"mask(this, mphone);" %>
    </div>


    <div class="col-lg-4 form-group">   
    <%= form.label :cliente_id, :class => 'control-label ' %>
    <%= form.text_field :cliente_id,  :class => 'form-control' %>
</div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
